(function(root){"use strict";const TIME_ZONE="America/Chicago",BLOCK_SECONDS=7200,BREAK_AFTER_CONTENT_SECONDS=[1500,3000,4500],SPOTS_PER_BREAK=3,DEFAULT_SPOT_SECONDS=60;function stationParts(date){const parts=new Intl.DateTimeFormat("en-US",{timeZone:TIME_ZONE,year:"numeric",month:"2-digit",day:"2-digit",hour:"2-digit",minute:"2-digit",second:"2-digit",hourCycle:"h23"}).formatToParts(date);return Object.fromEntries(parts.filter(p=>p.type!=="literal").map(p=>[p.type,Number(p.value)]))}function zonedToUtc(year,month,day,hour=0,minute=0,second=0){const target=Date.UTC(year,month-1,day,hour,minute,second);let guess=target;for(let i=0;i<4;i++){const p=stationParts(new Date(guess)),represented=Date.UTC(p.year,p.month-1,p.day,p.hour,p.minute,p.second);guess+=target-represented}return guess}function dateKey(nowMs){const p=stationParts(new Date(nowMs));return`${p.year}-${String(p.month).padStart(2,"0")}-${String(p.day).padStart(2,"0")}`}function hash(text){let value=2166136261;for(let i=0;i<text.length;i++)value=Math.imul(value^text.charCodeAt(i),16777619);return value>>>0}function seededShuffle(items,seedText){const copy=items.slice();let seed=hash(seedText);const random=()=>{seed+=0x6D2B79F5;let t=seed;t=Math.imul(t^(t>>>15),t|1);t^=t+Math.imul(t^(t>>>7),t|61);return((t^(t>>>14))>>>0)/4294967296};for(let i=copy.length-1;i>0;i--){const j=Math.floor(random()*(i+1));[copy[i],copy[j]]=[copy[j],copy[i]]}return copy}function movieRuntimeSeconds(movie){return Math.max(60,Math.floor(Number(movie&&movie.runtimeSeconds)||6000))}function breakDurationSeconds(commercials,breakIndex){if(!Array.isArray(commercials)||!commercials.length)return SPOTS_PER_BREAK*DEFAULT_SPOT_SECONDS;let seconds=0;for(let spot=0;spot<SPOTS_PER_BREAK;spot++){const ad=commercials[(breakIndex*SPOTS_PER_BREAK+spot)%commercials.length]||{};seconds+=Math.max(1,Math.floor(Number(ad.durationSeconds)||DEFAULT_SPOT_SECONDS))}return seconds}function fittedBreaks(movie,commercials){const runtime=movieRuntimeSeconds(movie);let used=runtime;const selected=[];BREAK_AFTER_CONTENT_SECONDS.filter(n=>n<runtime).forEach((boundary,index)=>{const duration=breakDurationSeconds(commercials,index);if(used+duration<=BLOCK_SECONDS){selected.push({boundary,index,duration});used+=duration}});return selected}function stationDurationSeconds(){return BLOCK_SECONDS}function masterCatalog(fallback){const candidates=[root.HERMIT_CATALOG,root.STAR_LAUNCHER_CATALOG];for(const list of candidates)if(Array.isArray(list)&&list.length)return list;return fallback}function eligibleMovie(movie){const runtime=Number(movie&&movie.runtimeSeconds);return!!(movie&&movie.cleared&&movie.videoId&&Number.isFinite(runtime)&&runtime>=3600&&runtime<=BLOCK_SECONDS)}function createDaySchedule(nowMs,catalog){const p=stationParts(new Date(nowMs)),midnightMs=zonedToUtc(p.year,p.month,p.day),passed=(Array.isArray(catalog)?catalog:[]).filter(eligibleMovie),allowedIds=new Set(passed.map(movie=>movie.videoId)),master=masterCatalog(catalog).filter(eligibleMovie);if(!master.length)throw new Error("No verified feature-length movies fit the two-hour channel slot.");const todayKey=dateKey(nowMs);let shuffled=seededShuffle(master,todayKey);const previous=seededShuffle(master,dateKey(midnightMs-1000));if(shuffled.length>1&&(shuffled[0]===previous[0]||shuffled.map(item=>item.videoId||item.title).join("|")===previous.map(item=>item.videoId||item.title).join("|"))){const firstDifferent=shuffled.findIndex(item=>(item.videoId||item.title)!==(previous[0].videoId||previous[0].title)),offset=firstDifferent>0?firstDifferent:1;shuffled=[...shuffled.slice(offset),...shuffled.slice(0,offset)]}return Array.from({length:12},(_,index)=>{const baseMovie=shuffled[index%shuffled.length],movie=allowedIds.size&&!allowedIds.has(baseMovie.videoId)?{...baseMovie,cleared:false}:baseMovie,startsAtMs=midnightMs+index*BLOCK_SECONDS*1000;return{id:`${todayKey}-${String(index).padStart(2,"0")}`,movie,startsAtMs,endsAtMs:startsAtMs+BLOCK_SECONDS*1000,blockSeconds:BLOCK_SECONDS,fullStationSeconds:BLOCK_SECONDS}})}function createSegments(block,commercials){const runtime=movieRuntimeSeconds(block.movie),selectedBreaks=fittedBreaks(block.movie,commercials),boundaries=[0,...selectedBreaks.map(item=>item.boundary),runtime],ads=Array.isArray(commercials)?commercials:[],segments=[];let stationOffset=0,adIndex=0;function pushSegment(segment,requestedDuration){const remaining=BLOCK_SECONDS-stationOffset;if(remaining<=0)return false;const duration=Math.min(Math.max(1,Math.floor(requestedDuration)),remaining);segments.push({...segment,stationStart:stationOffset,duration});stationOffset+=duration;return duration===requestedDuration}outer:for(let i=0;i<boundaries.length-1;i++){const sourceStart=boundaries[i];if(!pushSegment({kind:"movie",title:block.movie.title,videoId:block.movie.videoId,cleared:block.movie.cleared,sourceStart},boundaries[i+1]-sourceStart))break;if(i<selectedBreaks.length){for(let spot=0;spot<SPOTS_PER_BREAK;spot++){const ad=ads.length?(ads[adIndex++%ads.length]||{}):{title:"Station break",durationSeconds:DEFAULT_SPOT_SECONDS,videoId:"",cleared:false};if(!pushSegment({kind:"commercial",title:ad.title||"Station break",videoId:ad.videoId||"",cleared:!!ad.cleared,sourceStart:0},Math.max(1,Math.floor(Number(ad.durationSeconds)||DEFAULT_SPOT_SECONDS))))break outer}}}if(stationOffset<BLOCK_SECONDS)pushSegment({kind:"station",title:"Next movie starts at the next two-hour slot",videoId:"",cleared:true,sourceStart:0},BLOCK_SECONDS-stationOffset);return segments}function resolve(nowMs,schedule,commercials){const block=schedule.find(item=>nowMs>=item.startsAtMs&&nowMs<item.endsAtMs)||schedule[schedule.length-1]||schedule[0],blockElapsed=Math.max(0,Math.min(BLOCK_SECONDS-1,Math.floor((nowMs-block.startsAtMs)/1000))),segments=createSegments(block,commercials),segment=segments.find(item=>blockElapsed>=item.stationStart&&blockElapsed<item.stationStart+item.duration)||segments[segments.length-1],segmentElapsed=Math.max(0,blockElapsed-segment.stationStart),segmentIndex=segments.indexOf(segment);let movieReturnsIn=segment.duration-segmentElapsed;for(let i=segmentIndex+1;segment.kind==="commercial"&&i<segments.length&&segments[i].kind==="commercial";i++)movieReturnsIn+=segments[i].duration;return{block,segment,segmentElapsed,blockElapsed,mediaSeconds:segment.sourceStart+segmentElapsed,segmentRemaining:Math.max(0,segment.duration-segmentElapsed),movieReturnsIn:Math.max(0,movieReturnsIn),blockRemaining:Math.max(0,BLOCK_SECONDS-blockElapsed)}}root.HermitEngine={TIME_ZONE,BLOCK_SECONDS,stationParts,zonedToUtc,dateKey,stationDurationSeconds,createDaySchedule,createSegments,resolve}})(window);